language: python
python:
  - '2.7'

install:
  - python setup.py install

script:
  - cd ./python_tools/test
  - python -m unittest discover -s . -t .

# script:
# - python setup.py install
# cd python_tools/test
# python -m unittest discover
