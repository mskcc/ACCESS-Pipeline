language: python
python:
  - "2.7"
#  - "nightly"

install:
  - pip install -r requirements.txt
  - git clone https://github.com/mskcc/Innovation-QC.git

script:
  # Run all tests
  - for script in ./test/**/*.sh ; chmod 700 $script ; done
  - chmod 700 ./run_tests.sh
  - ./run_tests.sh /tmp
