language: python
python:
  - "2.7"
#  - "nightly"

install:
  - pip install -r requirements.txt
  - git clone https://github.com/mskcc/Innovation-QC.git

script:
  # Run all tests
  - for each in ./test/**/*.sh ; chmod 700 $each ; done
  - chmod 700 ./run_tests.sh
  - ./run_tests.sh
