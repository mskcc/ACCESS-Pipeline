language: python
python:
  - "2.7"
#  - "nightly"

install:
  - pip install -r requirements.txt

script:
  # Run all tests
  - find . -name *.sh -exec chmod 700 {} && {}\

  - chmod 700 test/run-pipeline-test.sh
  - test/run-pipeline-test.sh
