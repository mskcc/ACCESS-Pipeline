name: delly_params
type: record
fields:
  # For SV calling
  excluded_regions: File
  # For SV filtering
  filter_mode: string
  min_genotype_fraction: int
  min_fraction_alt_support: float
  min_sv_size: int
  passing_filter_sites: boolean
