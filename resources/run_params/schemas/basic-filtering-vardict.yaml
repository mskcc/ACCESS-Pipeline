name: basicfiltering_vardict_params
type: record
fields:
  verbose: boolean?
  total_depth: int
  allele_depth: int
  tumor_normal_ratio: int
  variant_fraction: float
  min_qual: int
  outdir: string
