name: bcftools_params
type: record
fields:
  allow_overlaps: boolean
  rm_dups: string

  # Optional
  threads: int?
  compact_PS: string?
  remove_duplicates: boolean?
  ligate: string?
  output_type: string?
  no_version: string?
  naive: string?
  min_PQ: int?
  regions_file: File?
  regions: string?
  output: string?
  list: string?